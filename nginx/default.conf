# Redirect HTTP to HTTPS (uncomment after SSL is set up)
# server {
#     listen 80;
#     server_name cohoivieclam.io.vn;
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# HTTPS server (uncomment after SSL is set up)
# server {
#     listen 443 ssl;
#     server_name cohoivieclam.io.vn;
#
#     ssl_certificate /etc/letsencrypt/live/cohoivieclam.io.vn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/cohoivieclam.io.vn/privkey.pem;
#
#     # Include the location block below
# }

# HTTP server (use this initially, before SSL)
server {
    listen 80;
    server_name cohoivieclam.io.vn;

    client_max_body_size 20M;

    # For Certbot SSL verification
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;
    gzip_min_length 1000;

    location / {
        proxy_pass http://app:8083;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        proxy_pass http://app:8083;
        proxy_set_header Host $host;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
